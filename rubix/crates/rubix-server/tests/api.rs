//! API integration tests, one module per resource.

mod api_tests {
    mod harness;

    mod agent;
    mod bus;
    mod command;
    mod flow;
    mod his;
    mod points;
    mod query;
    mod rollup;
    mod sites;
    mod sparks;
    mod tools;
}
