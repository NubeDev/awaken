//! API integration tests, one module per resource.

mod api_tests {
    mod harness;

    mod agent;
    mod agent_call;
    mod boards;
    mod bus;
    mod command;
    mod dispatch;
    mod flow;
    mod his;
    mod points;
    mod query;
    mod rollup;
    mod runs;
    mod sites;
    mod sparks;
    mod tools;
    mod widgets;
}
