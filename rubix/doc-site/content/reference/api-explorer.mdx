---
title: API Explorer
---

import ApiExplorer from '../../components/api-explorer.jsx'

# API Explorer

The interactive reference below is generated from the server's OpenAPI 3.1
document — the same spec served at `GET /api-docs/openapi.json`. Browse every
endpoint, inspect request and response schemas, and try calls against a running
server.

<ApiExplorer />
