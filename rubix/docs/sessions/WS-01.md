# WS-01 — `agent_call` board node → embedded agent

Status: Pending
Verified: (re-verify on first action)

## Goal

Close the one explicit `[ ]` in STATUS.md "Engine: reflow boards": the `agent_call` actor — a board
step that invokes the embedded awaken agent and feeds its result back into the graph. Make a board
able to escalate to the agent mid-evaluation, not just fire-and-forget.

## Current state (re-grep before coding — STATUS.md may be stale)

- `rubix/crates/rubix-flow/src/node/agent_call.rs` — the node **exists**; STATUS.md describes it as
  "raises an embedded-agent run via `PointAccess::request_agent`, fire-and-forget". Confirm whether
  it already returns the run result downstream or is genuinely fire-and-forget.
- `rubix/crates/rubix-flow/src/port.rs` — `PointAccess` trait (`request_agent` lives here).
- `rubix/crates/rubix-server/src/agent/runtime.rs` — the embedded `AgentRuntime` wiring.
- `rubix/crates/rubix-server/tests/api_tests/agent_call.rs` — existing test; extend, don't dupe.

If the node is already wired result-returning and tested, this WS's remaining scope is narrowed to
whatever the re-verify shows missing (e.g. surfacing the agent's structured output on an outport,
or a board-run integration test that asserts the agent's decision drives a downstream write). Update
this doc's `Verified:` line with what you found, then implement only the genuine gap.

## Scope / deliverables

- `agent_call` node returns the agent run outcome (or its gated decision) on an outport so downstream
  nodes can branch on it.
- A `StorePointAccess::request_agent` path that runs the embedded agent synchronously enough for the
  single-shot `BoardGraph::run()` to collect the result (respect the existing settle model).
- Integration test: a board with `read_point → agent_call → write_point` where the agent's decision
  reaches the store through the priority array (gating still enforced).

## Done definition

`cargo test --workspace` + `cargo clippy --workspace --all-targets` green; node result observable in
a board run; STATUS.md "Engine: reflow boards" `agent_call` item checked; session log `Done`.
