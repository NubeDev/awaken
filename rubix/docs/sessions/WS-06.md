# WS-06 — Auth: OIDC/JWT middleware + RBAC org→team→site

Status: In-progress
Started: 2026-06-12T12:10:30Z
Finished:
Verified: 2026-06-12 — all "Current state" file:line claims hold. `api/mod.rs`
router assembly, `lib.rs` `AppState` (profile/store/bus/query/his_tier/agent +
priority bands), `main.rs` boots the profile (`profile.auth_required` already a
cloud default, currently unused), `capability.rs` `Capability::covers` is the
path-boundary site-scoping helper RBAC aligns with.
Depends on: WS-04 (profile seam; auth-required is a cloud-profile default)

## Task breakdown (executed)

1. RBAC core: `Scope` (org→team→site, path-boundary `covers` reusing the
   `Capability` matching shape) + `Principal` (subject + scope + role).
2. Auth config from env (`RUBIX_OIDC_ISSUER`/`RUBIX_OIDC_JWKS`); fail-closed when
   the cloud profile requires auth but no issuer is configured.
3. JWT bearer verification against a boot-fetched JWKS (RS256), issuer/exp check,
   claims → `Principal`.
4. PATs / service accounts: `tokens` table (sha-256 hashed secret + scope), issue
   / list / revoke, bearer path accepts a PAT as an OIDC-JWT alternative.
5. Middleware: extract bearer, verify (JWT or PAT), attach `Principal`; enforced
   on the cloud profile, bypassed on edge.
6. RBAC gate helper for domain routes + OpenAPI security scheme + tests.

## Goal

STATUS.md "Platform / deployment": no auth present. STACK-DEISGN.md cloud node: "axum: API · auth
(OIDC/RBAC)". Ship JWT bearer validation middleware (OIDC-issued tokens, JWKS verification), an
org→team→site RBAC model, plus PATs/service accounts for non-interactive callers (drivers, the
embedded agent's own surface).

## Current state (re-grep before coding)

- `rubix/crates/rubix-server/src/api/mod.rs` — router assembly; middleware layer goes here.
- `rubix/crates/rubix-server/src/lib.rs` — `AppState`; carries the auth config + verifier.
- `rubix/crates/rubix-server/src/main.rs` — boot; auth required under the cloud profile (WS-04),
  optional/off under edge so local/offline nodes keep working.
- `rubix/crates/rubix-driver/src/manifest/capability.rs` — existing capability model; RBAC site
  scoping should align with `Capability::covers` rather than inventing a parallel scheme.

## Scope / deliverables

- JWT bearer middleware: validate signature against a configured JWKS/issuer (`RUBIX_OIDC_ISSUER`/
  `RUBIX_OIDC_JWKS`), extract subject + org/team/site claims, attach a `Principal` to request
  extensions. Fail-closed on the cloud profile, bypass on edge when auth is disabled.
- RBAC: org→team→site hierarchy; an authorization check helper that gates each domain route by the
  principal's scope (reuse keyexpr/site scoping). Agent writes already gate by priority — keep that;
  add the who-can-call-it layer above it.
- PATs / service accounts: a `tokens` table + issue/revoke; bearer path accepts a PAT as an
  alternative to an OIDC JWT.
- OpenAPI: mark routes' security scheme; tests for allow/deny across scopes + PAT path.

## Done definition

Green tests+clippy (edge: auth off, unchanged; cloud: auth enforced); RBAC denies cross-scope access;
PATs work; STATUS.md auth item checked; env list updated; session log `Done`.
