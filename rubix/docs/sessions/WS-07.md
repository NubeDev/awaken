# WS-07 — Tenancy: org/site → awaken `ScopeId`

Status: Pending
Verified: (re-verify on first action)
Depends on: WS-06 (org→team→site model + Principal)

## Goal

STATUS.md AI layer: "Tenancy: org/site hierarchy mirrored into awaken `ScopeId`." Make the embedded
agent and dispatch runs tenant-scoped — a run carries the org/site it acts within, mapped onto
awaken's `ScopeId`, so an agent invoked for one tenant cannot read/write another's points even
through its tools.

## Current state (re-grep before coding)

- `rubix/crates/rubix-server/src/agent/runtime.rs` — embedded `AgentRuntime`; where a run is
  activated. `ScopeId` comes from awaken-runtime-contract 0.6.
- `rubix/crates/rubix-server/src/dispatch/run.rs` — dispatch builds a `RunActivation` per spark;
  today keyed by spark id. Add the tenant scope from the spark's `{org}/{site}`.
- `rubix/crates/rubix-tools/src/tool/mod.rs` + each tool — tools resolve points by keyexpr; the
  scope must constrain which `{org}/{site}` prefixes a tool call may touch (reuse `Capability::covers`).
- WS-06's `Principal` (for chat-initiated runs) supplies the scope for `POST /agent/chat`.

## Scope / deliverables

- Map org/site → `ScopeId` (a deterministic, documented mapping).
- Thread the scope into `RunActivation` for both chat (`Principal` scope) and dispatch (spark
  `{org}/{site}`); the run's tools enforce it — a write/read/query/run_board outside the scope is
  refused at the tool boundary, not just the HTTP layer.
- Tests: an agent scoped to site A is refused a tool call against site B (chat path and dispatch path).

## Done definition

Green tests+clippy; scoped runs cannot cross tenants via tools; STATUS.md tenancy item checked;
session log `Done`.
