# WS-08 — Tracing spans on the bus

Status: Pending
Started:
Finished:

## Goal
Answer "how data/decisions flowed": spans emitted onto the event bus as work flows
ingest → pre-process → rule → insight → sink, correlated by the WS-05
trace/correlation id (SCOPE "Tracing", contract #3). Spans persist to SurrealDB
with bounded/rolling retention and a configurable sampling rate — high volume, not
kept forever (contract #4). This WS provides the hook for the Rhai per-evaluation
span tree; WS-11 fills that tree ("why did this fire").

## Dependencies
- **WS-05** — the correlation/trace id minted at the gate that correlates spans.
- **WS-07** — the event bus spans are emitted onto.
- **WS-01** — `rubix-store` for bounded/sampled span persistence.

## Scope / deliverables
- Crate(s): `rubix-trace`.
- A span model: trace id, parent span id, name, attributes, start/end — minimal,
  named by concept (not a `types.rs`).
- Emit spans onto the WS-07 bus as work flows; spans carry the WS-05 correlation
  id as their trace id.
- Persist spans to a SurrealDB `trace` table with bounded/rolling retention and a
  configurable sampling rate, env `RUBIX_TRACE_SAMPLE` (drop fraction before
  persist). Trace writes are append-only.
- A span-tree assembly read path keyed by trace id (assemble parent/child from
  stored spans) — the hook WS-11's Rhai span tree plugs into.
- Load-bearing contracts honored (STACK-DEISGN.md): #3 (correlation id is the
  trace id), #4 (trace append-only, bounded/sampled).

## Proposed file layout (verb-per-file, ≤400 lines)
```
crates/rubix-trace/src/
  lib.rs                     # barrel
  error.rs                   # trace-domain errors
  span.rs                    # Span (trace id, parent, name, attrs, start/end)
  emit.rs                    # emit a span onto the WS-07 bus
  sample.rs                  # sampling decision (RUBIX_TRACE_SAMPLE)
  persist.rs                 # append-only write to the trace table
  retain.rs                  # rolling retention bound enforcement
  assemble.rs                # read path: build the span tree by trace id
crates/rubix-trace/tests/
  emit_test.rs
  sample_test.rs             # drops at the configured rate
  retain_test.rs             # retention bound enforced
  assemble_test.rs           # span tree retrievable by trace id
```

## Done definition
Green `cargo test --workspace` + `cargo clippy --workspace --all-targets` clean; a
traced operation emits spans onto the bus and persists them so a span tree is
retrievable by trace id; sampling drops spans at the configured `RUBIX_TRACE_SAMPLE`
rate; the retention bound is enforced (oldest spans evicted past the bound)
(verified on kv-mem); the matching lines in
/home/user/code/rust/awaken/rubix/STATUS.md flipped [ ]→[x]; session log
Status: Done + Finished timestamp.

## Tests
- Unit (`#[cfg(test)]`): `sample` admits/drops at the configured rate over a large
  sample; `Span` parent/child linkage is well-formed; trace id equals the supplied
  correlation id.
- Integration (`tests/`, SurrealDB kv-mem): emit a multi-span operation on the
  WS-07 bus, persist, then `assemble` returns the full tree by trace id; set a low
  retention bound and assert spans past it are evicted; set sample=0 and assert
  nothing persists, sample=1 and assert all spans persist.
