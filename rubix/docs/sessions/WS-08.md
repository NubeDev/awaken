# WS-08 — Outbound MCP adapter (BMS tools to external agents)

Status: Pending
Verified: (re-verify on first action)
Depends on: WS-02 (run registry, for run-producing tools), WS-07 (scope enforcement)

## Goal

STATUS.md AI layer "Outbound adapters: MCP / A2A / AG-UI expose the building to external agents with
the same gating. None present." Ship the **MCP** adapter first: an MCP server surface that exposes
the same BMS tools (read_point / write_point / query / run_board) to external agents, with identical
priority-array gating and (WS-07) tenant scoping.

## Current state (re-grep before coding)

- `rubix/crates/rubix-tools/src/tool/mod.rs` + `write_point.rs` / `run_board.rs` — the `TypedTool`
  set already built for the embedded agent. The MCP adapter exposes THESE — do not reimplement the
  tools, wrap them.
- `rubix/crates/rubix-server/src/tools/registry.rs` — `build_tools(&AppState)`; the same registry
  feeds the MCP surface.
- `rubix/crates/rubix-server/src/api/mod.rs` — where the MCP transport (HTTP/SSE) mounts.

## Scope / deliverables

- An MCP server endpoint (HTTP/SSE transport) advertising the tool schemas and dispatching tool calls
  into the existing `build_tools` registry — same gating, same scope (WS-07), same suspend/HITL
  behavior (WS-02 run registry tracks externally-initiated runs).
- Auth: external callers authenticate via WS-06 (PAT/service account); the principal's scope binds
  the tool calls.
- Tests: an MCP tool call writes through the priority array (gated), a below-floor write is refused,
  an escalation-band write suspends and appears in the run registry.

## Done definition

Green tests+clippy; external MCP tool calls honor gating + scope; STATUS.md outbound-adapters item
updated (MCP shipped; A2A/AG-UI noted as remaining); session log `Done`.
