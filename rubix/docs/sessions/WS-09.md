# WS-09 — Scoped zenoh session per driver + reference driver binary

Status: Pending
Verified: (re-verify on first action)

## Goal

STATUS.md "Driver runtime": capability enforcement is at the server's queryables (owned-site scoped),
but **a scoped zenoh session per driver** (the driver's own session limited to its granted prefixes,
publish/subscribe gating) is still remaining. STATUS.md also lists "A reference driver binary" — note
a `rubix-driver-sim` crate already exists; re-verify what it does before adding.

## Current state (re-grep before coding — STATUS.md is partly stale here)

- `rubix/crates/rubix-driver-sim/` — **already exists** (`src/{main,config,liveliness,simulate}.rs`,
  `tests/supervised.rs`). Re-verify: is this the reference driver? Does it open a scoped session or a
  full one? Update the WS scope to the genuine gap.
- `rubix/crates/rubix-driver/src/manifest/capability.rs` — `CapabilitySet` / `Capability::covers`,
  the publish/subscribe gating model already defined.
- `rubix/crates/rubix-driver/src/manifest/env.rs` — env-injected identity/caps/config the supervisor
  passes to a child (`rubix/crates/rubix-server/src/supervisor/spawn.rs`).
- `rubix/crates/rubix-server/src/supervisor/` — spawn/health/reap/backoff already built.

## Scope / deliverables

- A driver-side helper (in `rubix-driver` or the sim) that opens a zenoh session and **enforces the
  granted `CapabilitySet` on that session**: a publish/subscribe to a keyexpr outside the grant is
  refused locally, before it hits the bus.
- The reference driver (`rubix-driver-sim`, extended if it doesn't already) uses that scoped session:
  it publishes `cur` only under its granted prefixes and rejects out-of-scope writes.
- A live end-to-end test: supervisor spawns the reference driver, the driver publishes a `cur` under
  its grant (reaches the server), and an out-of-grant publish is refused. (STATUS.md "Known issues":
  no live end-to-end supervisor spawn/restart test exists — this closes it.)

## Done definition

Green tests+clippy; driver session enforces caps locally; live spawn→publish→scope-refusal tested;
STATUS.md driver-runtime capability-at-the-bus item + reference-driver item updated; log `Done`.
