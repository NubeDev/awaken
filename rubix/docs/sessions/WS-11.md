# WS-11 — Rhai rules / insights runtime

Status: Pending
Started:
Finished:

## Goal
Deliver the embedded, deterministic rule/insight runtime (SCOPE "Rhai — rules and
insights"). Rules are composable (rules call rules), fire offline, consume the
time-window math from DataFusion (WS-09), and produce a decision/insight recorded
back to SurrealDB through the WS-05 gate, published as a WS-07 data-change event,
and accompanied by a WS-08 span tree per evaluation (the deterministic "why did
this fire"). Rhai owns the decision; DataFusion owns the data — heavy aggregation
never lives in Rhai.

## Dependencies
WS-09 (committed) for window values as rule inputs; WS-05 (committed) to record
the insight through the command gate (correlation id, audit/undo capture); WS-07
(committed) to publish the data-change event on the bus; WS-08 (committed) to
emit the per-eval span tree. Consumes the gate command path, the bus publish
seam, and the trace span emitter.

## Scope / deliverables
- Crate(s): `rubix-rules`.
- An embedded Rhai engine.
- A `Rule` model: script + input bindings + output insight shape.
- Composition: a rule invokes another rule.
- Evaluation that pulls DataFusion window values, runs the script, records the
  insight via the gate, publishes on the bus, and emits the span tree — carrying
  one correlation id through the whole eval.
- Load-bearing contracts honored (STACK-DEISGN.md): #3 (correlation id threaded
  through the eval and stamped into insight + event + spans); SCOPE "Rhai owns
  the decision; DataFusion owns the data".

## Proposed file layout (verb-per-file, ≤400 lines)
```
crates/rubix-rules/src/
  engine/
    mod.rs                 # barrel + Engine (Rhai setup)
    register.rs            # register host fns (window pull, invoke sub-rule)
  rule/
    mod.rs                 # barrel + Rule (script + bindings + insight shape)
    bind.rs                # resolve input bindings to DataFusion window values
  evaluate/
    mod.rs                 # barrel orchestrating the eval phases
    run.rs                 # execute the Rhai script -> decision
    compose.rs             # rule-calls-rule invocation
    record.rs              # write the insight via the WS-05 gate
    publish.rs             # emit the data-change event on the WS-07 bus
    span.rs                # build the WS-08 per-eval span tree
crates/rubix-rules/tests/
  evaluate/
    run_test.rs
    compose_test.rs
    record_test.rs
    span_test.rs
```

## Done definition
Green `cargo test --workspace` + `cargo clippy --workspace --all-targets` clean;
a rule fires offline on a window input, records an insight row, publishes the
event, and produces a retrievable span tree showing sub-rules + values +
decision; a composed rule (rule-calls-rule) works with no cloud dependency; the
matching lines in /home/user/code/rust/awaken/rubix/STATUS.md flipped [ ]→[x];
session log Status: Done + Finished timestamp.

## Tests
- Unit (`#[cfg(test)]`): a script with bound inputs evaluates to the expected
  decision; the span tree records each sub-rule, the values seen, and the
  decision; the correlation id is identical across insight, event, and spans.
- Integration (`tests/`, SurrealDB kv-mem, no cloud): feed a known window value,
  evaluate a rule, assert the insight row is recorded through the gate and a
  data-change event is published; evaluate a composed rule (rule-calls-rule) and
  assert the parent decision reflects the child and the span tree nests the
  sub-rule eval; retrieve the span tree and assert it shows the full "why".
