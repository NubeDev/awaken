# WS-01 — SQL interpolation engine + both query paths accept `variables`

Status: Done
Started: 2026-06-13T13:13:46Z
Finished: 2026-06-13T14:05:00Z
Verified: 2026-06-13 — re-grepped on first action and confirmed: `rubix-query` has
`context/ his/ provider/ rollup/ sql/ error.rs lib.rs` and **no** variable module (engine added as
new `interpolate/`); `crates/rubix-server/src/api/query/run.rs` request was `{ sql }` only and ran
`engine.query(&sql)`; `crates/rubix-server/src/api/datasources/run.rs` request was `{ sql, params,
fields }` and ran `executor.execute(&sql, &params)`; the read-only guard is `ensure_read_only` in
`rubix-query/src/sql/run.rs` (reused, not re-invented). DataFusion is `53.1.0` and binds positional
`$N` via `DataFrame::with_param_values(ParamValues::from(Vec<ScalarValue>))`. The `$N` placeholder
convention is shared by both backends, so the engine emits one neutral `Lowered { sql, params }`.

## Task breakdown / commits

1. `WS-01: feat(rubix-query): injection-safe SQL variable interpolation engine` — new
   `crates/rubix-query/src/interpolate/` (`var.rs` input types `QueryVariable`/`VarValue`/`Scalar`;
   `bound.rs` neutral `BoundParam`/`Lowered`; `error.rs` `InterpolateError`; `lower.rs` the token
   scanner; `mod.rs` barrel). Added `query_with_variables` on `QueryEngine` (lowers → binds via
   DataFusion `with_param_values`), `Interpolate` arm on `QueryError`, and an end-to-end test file
   (`tests/variables.rs`). 24 unit + 5 integration tests green.
2. `WS-01: feat(query): both query paths accept injection-safe `variables`` — extended
   `QueryRequest` and `DatasourceQueryRequest` with `variables: Vec<QueryVariable>` (`#[serde(default)]`
   → back-compat); both handlers lower the SQL before execution (datasource path numbers variable
   placeholders after its positional `params` via `start_index`, mapping `BoundParam` → datasource
   `Param`); registered the new DTOs + variable types in `api/openapi.rs`; added 4 `/query` API
   integration tests (bind/filter, injection-binds-as-literal, unknown-variable 400).

## Token forms shipped
`$name` / `${name}` → single bound `$N`; `${name:csv}` and `${name:singlequote}` → comma-joined
bound list (each value its own param); `$__sqlIn(name)` → `IN ($1, $2, …)`. An existing positional
`$1` is left untouched; an unknown/unbound/misused token is a clear `InterpolateError`, never a
silent passthrough. Every value binds — nothing is spliced into SQL text.

## Assumptions / deviations
- `${name:singlequote}` expands to the *same* bound `$N` list as `:csv` (the "quote" is the driver's
  job since the engine binds rather than splices) — quoting/escaping deliberately does not exist in
  the engine because no value ever touches SQL text. This is the design's injection boundary.
- `$__sqlIn(name)` emits the `IN` keyword (design §2 writes it as `IN ($1, …)`), so the caller writes
  `WHERE id $__sqlIn(ids)`.
- Did **not** touch `ui/src/api/types.ts`: rubix's UI authors no query-request DTO today, so there is
  nothing to mirror; the variable UI/model and any TS shape are WS-02's scope.

## Follow-ups
- WS-03 extends this engine with time macros (`$__from`/`$__to`/`$__timeFilter`/…) — designed as
  "add one token handler" in `lower.rs`, not a rewrite. `BoundParam` has no timestamp variant yet;
  WS-03 adds one (datasource `Param::Timestamp` already exists).

Design source: [variables-and-templating.md](../../design/variables-and-templating.md) §2.

## Goal

Rubix has **no SQL macro/variable binder**. Before any dashboard variable, time range, or page
context can drive a query, there must be one server-side, injection-safe interpolation engine that
lowers `$name`/`${name}` tokens into bound parameters. This WS builds exactly that engine and makes
both query entry points accept a `variables` payload. It ships nothing user-facing — it is the
contract WS-02/WS-03/WS-06 consume.

## Current state (re-grep before coding)

- `crates/rubix-query/` — exists today with `context/`, `provider/`, `rollup/`, `sql/`, `his/`,
  `error.rs`, `lib.rs`. It executes queries but has **no variable/interpolation module**. Add the
  engine here so both query paths share one binder.
- `crates/rubix-server/src/api/query/run.rs` — `POST /api/v1/query`, DataFusion over canonical
  tables, request is `{ sql }` only. Has the read-only / timeout / row-cap guards — **reuse them**.
- `crates/rubix-server/src/api/datasources/run.rs` — `POST /api/v1/datasources/{id}/query`, request
  is `{ sql, params: Vec<Value> }` (positional binds, no named interpolation).
- `crates/rubix-server/src/api/openapi.rs` — DTO registration for the new request fields.

## Scope / deliverables

- A new interpolation module in `rubix-query` (verb/concept-named files, ≤400 lines each):
  - `QueryVariable { name, value | values }` input type.
  - Lowering of, in SQL text, **before** execution:
    - `$name` / `${name}` → single value bound as a positional/`$N` parameter.
    - `${name:csv}` → comma-joined, **each value its own bound parameter**.
    - `${name:singlequote}` → quoted list for `IN`-style use, each value a bound param.
    - `$__sqlIn(name)` → safe `IN ($1, $2, …)` expansion from a multi-value variable.
  - An unknown/unbound token is a clear engine error, not a silent passthrough.
- **The injection boundary:** every value becomes a bound parameter, never concatenated. A value of
  `'); DROP TABLE points; --` binds as a literal. Quoting/escaping lives in the engine, server-side,
  mandatory. Do **not** invent a second read-only/timeout/row-cap guard — reuse the ones already on
  the two query paths.
- Extend `QueryRequest` and `DatasourceQueryRequest` with `variables: Vec<QueryVariable>` (default
  empty → existing behaviour unchanged, back-compat). Register the new field shape in `api/openapi.rs`.
- Wire both `run.rs` handlers to lower the SQL through the engine before execution.

## Done definition

Green `cargo test --workspace` + clippy clean; engine unit tests cover each substitution form,
multi-value expansion, and an injection attempt that binds-not-executes; both query paths accept and
apply `variables` with no payload meaning no change; OpenAPI surface compiles; session log `Done`.

## Notes / boundaries
- This WS does **not** add the `variables` field to the dashboard model or any UI — that is WS-02.
- It does **not** add the time macros (`$__from`/`$__timeFilter`/…) — those extend this engine in
  WS-03, but ship the engine so that extension is "add one token handler", not a rewrite.
