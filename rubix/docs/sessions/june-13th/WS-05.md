# WS-05 — Entity-tag store/routes/authz + `nav_nodes` table + nav CRUD + `nav_node` grant kind + default tree

Status: Done
Started: 2026-06-13T15:10:00Z
Finished: 2026-06-13T15:55:00Z
Verified: 2026-06-13T15:10:00Z — re-grepped the cited anchors. Drift found and reconciled:
- migrate.rs top `user_version` is **v6** (`migrate_v6_dashboard_variables`), not the v4 the design doc
  cites; this WS adds **v7** (entity_tags) + **v8** (nav_nodes). Crate paths are under
  `rubix/crates/rubix-server/...` (not `crates/...`).
- grant model: `api/grants/mod.rs` + `store/grants.rs` (`GrantRecord`, `Permission`, `SubjectKind`,
  `grants_for_subjects`); two-layer authz in `api/scope_auth.rs` (`may_read_resource`,
  `authorize_resource_write`, `resource_ref`). Reused; no parallel authz.
- dashboard delete path: `store/dashboards.rs::delete_dashboard_sqlite` (+ postgres mirror).
- org provision: `api/orgs/create.rs::provision_org`.
- `model.rs:121-146` (cited for dashboards) is `Dashboard` — confirmed `id` global PK + separate `org`
  column, so a dashboard-target FK can't encode org-safety (validated in handler, per spec).

Design source: [page-context-and-nav.md](../../design/page-context-and-nav.md) §§3,4,6.

## Goal

The page-context/nav **backend**: a behaviour-affecting entity-tag store, a nestable org-scoped
navigation tree where each node assigns a board + context payload, and per-node access via the
existing grant model. This is what lets one board mount at many nodes, each parameterising and gating
it.

## Current state (re-grep before coding)

- `crates/rubix-server/src/store/migrate.rs` — forward-only `user_version` migrations (re-grep the
  current top; the design doc says v4, the tree has drifted past that). Add new steps, dual-dialect.
- `crates/rubix-server/src/store/schema.rs` — SQLite base + `#[cfg(feature="cloud")]` Postgres.
- `crates/rubix-server/src/api/grants/mod.rs` — the grant/`resource_ref` model. Register `nav_node`
  as a grantable kind (`view | edit | delete`, org-scoped) here; do **not** invent a parallel authz.
- `crates/rubix-server/src/api/scope_auth.rs` + `docs/design/authz-rbac.md` — the two-layer authz the
  tag write/read handlers must reuse.
- `crates/rubix-server/src/store/dashboards.rs` — the dashboard delete path; sweep dependent nav
  nodes to `{kind:"group"}` here (losing a board must not delete the node).
- `crates/rubix-server/src/api/orgs/create.rs` — org provision; seed the default nav tree here.
- `crates/rubix-server/src/api/openapi.rs`, `ui/src/api/types.ts` — DTO + hand-authored TS mirror.

## Scope / deliverables

- **Entity-tag store** (new): org-scoped `(org, kind, entity_id, key, value)`; routes
  `PUT /api/v1/tags/{kind}/{id}` (full-replace), `GET /api/v1/tags/{kind}/{id}`,
  `GET /api/v1/tags/entities/{kind}` (reverse lookup), `GET /api/v1/tags/keys`. Because tags are now
  **behaviour-affecting**, write/read handlers resolve the target entity and enforce the **entity's
  own authz** (`edit` to write, `view`/read scope to read), and reject unknown/foreign ids. Sweep tags
  on entity delete (in the existing delete handlers).
- **`nav_nodes` table** via a forward-only `user_version` bump, dual-dialect, org-scoped, indexed on
  `(org, parent_id, sort_order)`. Shape: `id, org, parent_id?, title, sort_order, target jsonb,
  context? jsonb, icon?, accent?`. `target` is a tagged union — `{kind:"group"}` |
  `{kind:"dashboard", dashboard_id}` | `{kind:"route", route}`. The `route` value is a **closed
  allow-list** of built-in pages (sites|equips|points|dashboards|datasources|rules|boards|sparks|
  runs|audit|access). A `dashboard` target's id is validated **in the handler** to exist within the
  caller's **org** (a bare FK can't encode org-safety). On board delete, sweep dependent nodes to
  `{kind:"group"}`.
- **Nav CRUD + reorder/reparent** routes under `api/nav/**`; DTO registered; TS mirror authored.
- **`nav_node` grant kind:** register as grantable in the grant model. The nav-tree `GET` filters to
  nodes the principal holds `view` on (the existing per-node read-filter). Opening a node checks
  `view` on the **node**.
- **Default tree on org provision:** seed every static route as a node, granted org-wide, so gating is
  opt-in tightening — not a lockout. Static pages get access-gated for the first time.

## Done definition

Green `cargo test --workspace` + clippy; tests: nav CRUD + nest/reorder/reparent, org isolation +
cross-org dashboard-target rejection, tag authz (can't tag an un-editable or nonexistent id), board-
delete sweeps nodes to `group`, default tree seeded on a fresh org, `nav_node` grant view-filter; an
injected nav title / tag value binds-not-executes wherever it reaches SQL; OpenAPI compiles; log `Done`.

## Notes / boundaries
- The **`context` VariableKind, `PageContext` assembly, sidebar UI, Navigation builder, and the
  `varRevision` re-query wiring** are all **WS-06**.
- Nav create/update/delete/reorder + grant changes are recorded by **WS-08**'s recorder (no double-
  record); this WS just ships the mutations.

## What landed

- **Core model** (`rubix-core`, commit 09541e1d): `NavNode` / `NavTarget` (group|dashboard|route,
  tagged on `kind`) / `NavRoute` (closed 11-page allow-list) / `NavContext`; `EntityTags`
  (transparent map) / `TagEntityKind`. `NavNode::validate` rejects empty titles and context on a
  non-dashboard target.
- **Store + migrations** (`rubix-server`, commit 5beda3e7): `entity_tags` + `nav_nodes` tables in
  both `SCHEMA_SQLITE` and `SCHEMA_POSTGRES`, indexed `idx_entity_tags_reverse` /
  `idx_nav_nodes_tree`; forward-only `user_version` steps **v7** (entity_tags) + **v8** (nav_nodes);
  SQLite + Postgres store impls (replace/get/reverse/keys/sweep for tags; CRUD + dashboard-target
  sweep + same-org parent guard for nav). All free-form values bind as parameters.
- **API** (`rubix-server`, commit 3d20b583): `api/nav/**` (POST/GET/PATCH/DELETE + list) and
  `api/tags/**` (PUT/GET/{kind}/{id}, reverse, keys). Nav writes authorize a `nav_node` write
  (org-scoped, `nav_node:*` for create); the tree GET and single-node GET filter on `view` via the
  new `nav_node` grant kind in `scope_auth.rs`. Cross-org dashboard targets and foreign parents are
  404s by construction. Tag handlers enforce the **target entity's** own authz (dashboard edit/view).
  Dashboard delete sweeps dependent nodes to `group` and sweeps the entity's tags. Default tree
  ("Pages" group + one route node per `NavRoute::ALL`) seeded on first org provision; default
  visibility is the Layer-1 org-scope read (no grant rows seeded). OpenAPI paths + schemas and the
  hand-authored TS mirror (`ui/src/api/types.ts`) updated.

## Tests

15 API integration tests (`tests/api_tests/nav.rs` + `tags.rs`): nav CRUD/nest/reorder/reparent,
parent-cascade delete, self-parent reject, org isolation, cross-org dashboard-target reject,
same-org parent guard, `nav_node` grant view-filter (a foreign-org viewer with a single node grant
sees only that node; the ungranted node is a 404), default-tree seed on provision, injection-shaped
title binds-not-executes; tag PUT/GET full-replace + reverse lookup + key list, unknown-kind 404,
nonexistent-id 404, viewer-cannot-write, board-delete sweeps tags + nodes, injection-shaped value
binds. Plus the v6→v7/v8 migration round-trip test (`tests/migrate.rs`) from the store commit.
`rubix-server`: 172 api + 90 lib + 6 migrate green. Clippy clean on default and `--features cloud`.

## Assumptions / follow-ups

- "Granted org-wide" default-tree visibility is satisfied by the existing Layer-1 org-scope read
  (grants only ADD/narrow), matching the dashboard pattern — no explicit grant rows are seeded.
- `TagEntityKind` currently has the single `Dashboard` variant (the only behaviour-affecting tag
  surface today); the route + store key are kind-parameterised so adding a kind is additive.
- The nav `context` payload is stored and round-tripped but its **consumption** (PageContext
  assembly, variable resolution, sidebar/Navigation UI) is WS-06, as scoped.
- Unrelated pre-existing flake observed during the workspace run:
  `rubix-driver-sim::supervised::out_of_grant_publish_is_refused_locally` (real-zenoh-mesh 3s timing
  window) — no WS-05 code on that path; logged to TODOs.
