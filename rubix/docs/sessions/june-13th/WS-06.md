# WS-06 — `context` VariableKind + PageContext assembly + varRevision wiring + nested sidebar + Navigation builder

Status: Done
Started: 2026-06-13T15:17:26Z
Finished: 2026-06-13T15:41:05Z
Verified: 2026-06-13T15:17:26Z — re-grepped. Drifts from the spec's "Current state":
  - The `VariableKind`/`VariableConfig` enums live in `crates/rubix-core/src/variable.rs`, NOT
    `model.rs` (model split into variable.rs/nav.rs/entity_tag.rs/tags.rs). OpenAPI is utoipa-derived
    (`ToSchema` on the enum), so registration is automatic once the arm is added.
  - There is no `ui/src/features/variables/resolve.ts`; resolution lives in `use-resolution.ts`
    (`useVariableResolution`), ordering in `order.ts`, revision in `revision.ts`, ref-extraction in
    `refs.ts`, query-var build in `query-variables.ts`. The `context` arm folds into these.
  - WS-05 shipped nav/tag backends (routes under api/nav/** + api/tags/**) but NO frontend wiring:
    no nav/tag endpoints/keys/hooks/types entries beyond the DTO mirrors. WS-06 adds that wiring.
  - The flat sidebar is `components/layout/app-sidebar.tsx` (uses `scopedNavGroups`); `features/nav/`
    does not exist yet.

Design source: [page-context-and-nav.md](../../design/page-context-and-nav.md) §§1,2,5,7.

Depends on: **WS-02** (variable engine/UI + `?var-*`), **WS-04** (the `varRevision` re-query path),
**WS-05** (nav + tag backends).

## Goal

The page-context **frontend + the `context` variable source**: a page reads its URL / nav-node
position / tags and feeds those into variables, so one board rendered under Building-1 queries
Building-1's data and under Building-2 queries Building-2's — no second board.

## Current state (re-grep before coding)

- `ui/src/components/layout/app-sidebar.tsx` — flat scope-derived sidebar (`scopedNavGroups(...)`).
  Replace the flat dashboards list with the nested tree.
- `ui/src/context/scope-provider.tsx` — resolves `org` + `siteSlug` from the route; no bare-param /
  nav-node / tag source today.
- `crates/rubix-core/src/model.rs` — the `VariableKind` enum (WS-02); add the `context` arm + config.
- `api/openapi.rs` + `ui/src/api/types.ts` — register/mirror the `context` kind.
- `ui/src/features/variables/resolve.ts` + the `varRevision` key (WS-02/WS-04) — fold context in here.

## Scope / deliverables

- **`context` VariableKind (full-stack):** Rust DTO enum arm + config struct in `model.rs`, OpenAPI
  registration, TS union + parser + `resolve.ts` arm + editor form, export/import, tests at each
  layer. Config `{ source: 'nav'|'url'|'tag'|'values', key }`. Built-in tokens (no authoring):
  `$__nav_slug`, `$__nav_name`, `$__tag(key)`. A `query` variable can reference a `context` variable
  as a parent — rides the same injection-safe binder, no new interpolation.
- **`PageContext` assembly** (`ui/src/features/variables/context.ts`, new): assemble from sources kept
  **separate** — `nav` (node_id/slug/name/path), `url` (bare params), `tags` (board tags merged under
  `NavNode.context.tags`), `scope` (existing org/site), `values` (`NavNode.context.values`).
  **Precedence (later wins) per name:** board tags → `nav.values` → URL params → explicit var-bar
  selection. `NavNode.context` is exactly `{ values?, tags? }` — not arbitrary keys. Context is
  read-only input to resolution, **never a 4th store / no `varOverrides` channel**.
- **Resolution wiring:** fold resolved context **into the variable values** that drive `varRevision`
  so a context change bumps the one revision like any selection; **re-key variable resolution** on the
  assembled context too (nav/tag/url) so two mounts of the same board don't serve each other's cached
  option lists. Reuse WS-02/WS-04's dependency-driven invalidation; do not add a parallel one.
- **`TagEditor`** mounted on the board (`kind="dashboard"`); tags feed `PageContext.tags`.
- **Nested sidebar** (`ui/src/features/nav/**`): collapsible tree replacing the flat list; `group`
  nodes expand/collapse, `dashboard`/`route` nodes are links; sidebar shows only nodes the user holds
  `view` on (server-filtered by WS-05).
- **Navigation builder + routing:** add/nest/reorder nodes, pick a target (board + context, or static
  route), manage grants inline. A `dashboard` node opens `…/dashboards/:slug?nav=:nodeId` → read
  `nav` → load node → merge `node.context` into `PageContext`; a `route` node navigates to the static
  page. Bare URL params (`?building=b1`) drive the `url` source for external deep-links — keep the
  `var-` prefix reserved (don't collide).

## Done definition

Green `cargo test --workspace` (for the model/DTO arm) + `pnpm -C ui build` + `test:unit`; one board
at two nodes with different `context.values` renders two sites' data; `NavNode.context` merges per the
precedence; changing the node bumps `varRevision` + re-keys resolution (no cross-mount cache bleed);
deep-links (`?nav=…`, bare `?building=…`) restore the view; an injected nav title / tag value binds-
not-executes; session log `Done`.

## Notes / boundaries
- The variable model/engine is **WS-02**; the nav/tag backend + grant kind + default tree is **WS-05**.
- Tag edits + nav changes are recorded by **WS-08** (tag edits under the `dashboard` kind) — no double-
  record here.
- Repeat-by-variable *rendering* is out of scope (nav is navigation, not in-canvas repeat).

## Landed (commits, in order)

- `9b2558b6` core: `context` arm on `VariableKind`/`VariableConfig` + `ContextSource` enum
  (`crates/rubix-core/src/variable.rs`, exported in lib.rs, registered in openapi.rs). Wire shape
  `{ "kind": "context", "source": "nav", "key": "slug" }`.
- `05dce2a9` engine + UI: `$__tag(key)` token in the interpolation engine
  (`crates/rubix-query/src/interpolate/lower.rs`, synthetic var `__tag(<key>)`, binds as a parameter —
  injection test `tag_token_value_with_sql_metachars_binds_never_executes`); `PageContext` assembly
  (`ui/src/features/variables/context.ts`) + `context` resolution arm folded into `use-resolution.ts`,
  `query-variables.ts`, `refs.ts`, `variable-editor*`; TS mirror in `api/types.ts`.
- `917ce97d` UI: nav + entity-tag API client (`endpoints.ts`, `keys.ts`, `use-nav.ts`,
  `use-entity-tags.ts`).
- `37775805` UI: nav tree assembly (`tree.ts`), `usePageContext` wiring into the builder, `TagEditor`
  on `kind="dashboard"`.
- `a67f356e` UI: nested collapsible nav-tree sidebar (`nav-tree.tsx`, mounted in `app-sidebar.tsx`,
  server-filtered to `view`-granted nodes) + `target-href.ts` resolver.
- `25886873` UI: Navigation builder (`navigation-builder.tsx`) — add/nest/reorder/reparent nodes
  (`reorder.ts`), pick target (group/board+`context.values`/static route) via `add-node-form.tsx` +
  `parse-values.ts`, manage view/edit grants inline (`node-grants.tsx`, `nav_node:<id>` refs). Routed
  at `settings/navigation`, gated on `whoami.can_admin`; linked from the Manage nav group.

## Gates (green)

- Rust: `cargo test -p rubix-core -p rubix-query` pass (incl. injection-binding tests); `rubix-server`
  builds; `cargo clippy -p rubix-core -p rubix-query -p rubix-server` clean.
- UI: `pnpm build` (tsc + vite) clean; `pnpm test:unit` 137/137; `pnpm lint` 0 errors (4 pre-existing
  react-refresh warnings in preferences-/scope-provider, not WS-06 files).

## Assumptions / deviations

- `parseValues` extracted from `add-node-form.tsx` into `parse-values.ts` (+ unit test) to keep the
  component a component-only export (avoids a new react-refresh lint warning) and to put the
  value-binding rule in one place.
- The nested `NavTree` renders **above** the existing flat scope nav (renders nothing when empty)
  rather than replacing it, so existing navigation other WSs depend on stays intact — additive/safe.
- Nav `context` source: node title used as `name`, board route slug as `slug`.

## Follow-ups (not blockers)

- Reparent in the builder is via the add-form parent select on create; in-place drag-reparent of an
  existing node is not wired (reorder up/down + delete are). The backend supports reparent (WS-05).
