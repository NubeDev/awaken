# WS-07 — `changes` + `undo_cursors` tables + `ChangeRecorder` + `Reversible` registry + cascade + coverage guard

Status: Done
Started: 2026-06-13T15:45:11Z
Finished: 2026-06-13T15:58:00Z
Verified: 2026-06-13T15:47:00Z — store/schema.rs had no audit/change table (confirmed); migrate.rs
top `user_version` was **v8** (v8 `nav_nodes`), not v4 as the design doc said — the tree drifted as
WS-05/WS-06 landed, so this WS adds **v9** (`changes` + `undo_cursors`). store/sites.rs cascade
delete confirmed at `delete_site_*` (WS-08 wires its grouped recording; substrate provides the
`group_id` API + grouped dispatch here).

Design source: [audit-and-undo.md](../../design/audit-and-undo.md) "The substrate", "Recording",
"Undo/Redo".

## Goal

Build the one append-only **change ledger** substrate that powers audit log + undo/redo "for
everything": the `changes` table, the per-actor `undo_cursors` table, the sole-write-path
`ChangeRecorder`, the `Reversible` registry with a generic snapshot reverser, and the coverage guard
that fails on a silently-unwired kind. This WS lands the substrate **without** wiring every handler —
that is WS-08.

## Current state (re-grep before coding)

- `crates/rubix-server/src/store/schema.rs` — **no audit/change table today** (the only "history" is
  the `his` time-series of point *values*, which is unrelated). Add `changes` + `undo_cursors`,
  dual-dialect (SQLite base + `#[cfg(feature="cloud")]` Postgres).
- `crates/rubix-server/src/store/migrate.rs` — forward-only `user_version` (re-grep the current top;
  the design doc says v4, the tree has drifted). Add a new step.
- `crates/rubix-server/src/store/sites.rs` — the site cascade delete (equips/points/history/sparks);
  the one place the generic reverser needs the owning handler's help (WS-08 wires it).

## Scope / deliverables

- **`changes` table** (append-only, one row per logical mutation): `id, at, org, site_id?, actor,
  kind, resource_id, op (Create|Update|Delete), before? (full JSON snapshot), after?, group_id,
  correlation?`. `Actor = User{subject} | Agent{run_id, model} | System`. Indexed on `(org, at)`,
  `(org, kind, resource_id)`, `group_id`. **Snapshot, not patch.**
- **`undo_cursors` table:** `(org, subject)` → cursor position, undo/redo is **per-actor**; a
  version/epoch column for CAS-guarded concurrent undo.
- **`ChangeRecorder`** (`store/changes.rs`, new) — the **sole** write path; `record(Change)` runs
  **inside the mutation's DB transaction** so the change commits atomically with the mutation.
- **`Reversible` registry:** `trait Reversible { kind(); apply_inverse(store, &Change); apply_forward
  (store, &Change); }`. A **generic snapshot reverser** covers most kinds mechanically (undo Create→
  delete, undo Delete→re-insert `before`, undo Update→write `before`; redo = forward). Custom impls
  only where a cascade/side-effect needs it.
- **Cascade grouping support:** the substrate must let a handler record **one row per deleted child
  under a shared `group_id`** so a subtree undoes as one step (WS-08 wires the site path; this WS
  provides the `group_id` API + the grouped-undo dispatch).
- **Coverage guard test:** enumerate registered reversible kinds; assert each has ≥1
  create/update/delete path producing a `changes` row, and `before` is non-null on update (catches
  recording outside the tx). A registered mutable kind with no recording path **fails the test**.

## Done definition

Green `cargo test --workspace` + clippy; the two tables land via a forward-only `user_version` bump,
dual-dialect, org-scoped; `ChangeRecorder` records inside a tx; the generic reverser round-trips a
snapshot create/update/delete; grouped undo replays a multi-row group atomically; the coverage guard
is green and deliberately unwiring a kind makes it fail; session log `Done`.

## Notes / boundaries
- **Wiring `record()` into each mutation handler, secret redaction, the agent actor, the audit-read
  routes, and the `/undo`/`/redo` HTTP surface are all WS-08.** This WS ships the substrate + the
  registry + the guard only.
- Point *value* writes (`cur`/`write`) are telemetry → `his`, **not** the audit ledger. Out of scope.

## What landed (commits)

1. `feat(core)`: `Change`/`Actor`/`Op` model in `rubix-core/src/change.rs` — full-snapshot
   `before`/`after`, op-consistent constructors, and `validate()` (rejects a `before`-less update,
   the type-level half of the coverage guard). Exported from `lib.rs`.
2. `feat(store)`: the substrate —
   - **Schema/migration v9** (dual-dialect SQLite + `cloud` Postgres): `changes` (id/at/org/site_id?/
     actor/kind/resource_id/op/before?/after?/group_id/correlation?, indexed `(org,at)`,
     `(org,kind,resource_id)`, `group_id`) and `undo_cursors` (`(org,subject)` PK → `redo_stack` JSON
     LIFO + `epoch` CAS). Forward-only `user_version` bump; mirrored in `schema.rs` + `migrate.rs` +
     the postgres `truncate_all` list.
   - **`store/changes.rs`** — the sole write path: `record_change` (own-tx) + `record_in_sqlite_tx`
     (caller-tx, for WS-08's in-handler atomic record), both op/snapshot-validated. Org-scoped audit
     reads (`list_changes` + `ChangeFilter`, `resource_changes`, `changes_in_group`), the per-actor
     cursor (`undo_cursor`/`cas_undo_cursor`/`clear_redo_stack`), and `newest_undoable_group`
     (per-actor, skips already-undone). All ids/timestamps bind as TEXT params (injection-safe).
   - **`store/reversible.rs`** — `Reversible` trait + `SnapshotKind`/`SnapshotReverser<K>` generic
     mechanical reverser (undo Create→delete, Delete→re-insert `before`, Update→write `before` back;
     redo = forward), `ReverserRegistry` (registers `dashboard`, fails closed on unknown kind),
     `registered_kinds`, and grouped `apply_group_inverse`/`apply_group_forward` dispatch.
   - Postgres mirror in `store/postgres/changes.rs`.
3. `test(store)`: `tests/change_ledger.rs` (reverser round-trips create/update/delete; grouped
   undo/redo; unknown-kind-fails-closed; coverage guard records every registered kind + catches an
   unwired one; audit filter + org isolation) and a v8→v9 migration test in `tests/migrate.rs`. Plus
   5 `#[cfg(test)]` units in `changes.rs` (tx commit/rollback, org scoping, CAS double-pop guard,
   per-actor `newest_undoable_group`).

## Assumptions / deviations
- **Cursor shape:** modelled the per-actor cursor as a `redo_stack` LIFO of undone `group_id`s +
  `epoch` (CAS), rather than a numeric position — interleaving edits would invalidate a numeric
  cursor, while the stack is correct under concurrent edits and gives standard "new edit clears redo"
  semantics (`clear_redo_stack`).
- **Agent/System actors** collapse onto stable cursor keys (`@agent`/`@system`) via
  `Actor::cursor_subject()`; per the design, agent/system changes are undone by the *user* through the
  audit surface, not a per-run cursor.
- **Registry scope:** WS-07 registers only the `dashboard` kind (the design's proof-of-substrate
  kind). WS-08 adds the remaining kinds + their recording paths; each new kind is one `SnapshotKind`
  impl (or a bespoke `Reversible` for a cascade/side-effect).

## Follow-ups (WS-08)
- Wire `record_in_sqlite_tx` (+ a Postgres tx variant) into each mutation handler, the
  `Actor::Agent` path, and secret redaction; register the remaining kinds; add the `GET /audit` read
  routes and `POST /undo`/`/redo` HTTP surface (popping the cursor via `cas_undo_cursor` +
  `apply_group_inverse`). The cascade-site delete records one group via `new_group_id()`.

## Gate
- `cargo test --workspace` green (the one `rubix-driver-sim out_of_grant_publish_is_refused_locally`
  failure is the known zenoh-mesh timing flake — passes on retry, no WS-07 code on that path; already
  in TODOs from WS-05). `cargo clippy --workspace --all-targets` clean for WS-07 files (the two remaining warnings
  are pre-existing: `prefs.rs` field-assignment + `tests/api.rs` expect-call). Cloud-feature build +
  clippy also clean.
