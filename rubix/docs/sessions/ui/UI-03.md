# UI-03 — Delete the demo layer; UI reads the network only

Status: Pending
Depends: UI-01 (verified types), UI-02 (seed exists — do NOT start before UI-02 is ✅)

## Goal

Remove every byte of fake data from the UI. After this row, the only data source is the HTTP API.

## Tasks

1. Delete `ui/src/api/demo/` entirely (fixtures + shims).
2. `ui/src/api/endpoints.ts`: strip every `isDemo() ? demo.… :` branch — each endpoint is a plain
   `request()` call again. Delete the `VITE_DEMO` env handling.
3. Sweep the hardcoded claims the demo era left behind:
   - `features/sparks/index.tsx`: `agentAttributed={s.rule === 'simultaneous-heat-cool'}` — the
     wire `Spark` has no agent-attribution field today. Drop the chip, or show it only from a
     real field if UI-01 found one. **Do not fake it.** If product wants it back, that's a
     backend field → `../TODOs.md` entry.
   - `components/profile-dropdown.tsx` + `components/layout/data/sidebar-data.ts` user block:
     remove "satnaing" remnants; show a neutral operator identity until auth lands (UI-07).
4. Empty/error states are now reachable in production paths — polish pass:
   - Every list/chart keeps its existing skeleton while loading (no layout jump).
   - Server-down: a single non-blocking banner/toast pattern (React Query `onError` already
     routes to sonner) — pages must not white-screen; panels show their honest empty state.
5. Tests: delete demo-dependent assumptions; keep/extend hook tests with mocked `fetch`
   (test-only fixtures allowed).

## Gates (all from STATUS.md DONE GATE, emphasized)

- **Grep gate goes live here**: `grep -rn "api/demo\|VITE_DEMO\|sample-board" ui/src
  --include='*.ts*'` → zero hits outside `*.test.*`. (`sample-board` falls in UI-04; if it still
  exists, the grep gate for this row excludes that one path — note it in the session log.)
- **Look-freeze**: with server+seed up, re-shoot all four surfaces and compare against
  `ui/docs/reference/`. Identical-or-better is the bar; any lost widget/series/density = not done.

## Done means

Kill the backend → UI shows honest empties. Start backend+seed → UI is pixel-equivalent to the
reference set. No demo code exists in `src/`.
