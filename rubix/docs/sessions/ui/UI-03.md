# UI-03 — Delete the demo layer; UI reads the network only

Status: Done
Started: 2026-06-12T23:05:15Z
Finished: 2026-06-12T23:12:00Z
Depends: UI-01 (verified types) ✅, UI-02 (seed) ✅

## Goal

Remove every byte of fake data from the UI. After this row the only data source
is the HTTP API.

## First-action re-grep (spec drift check)

Re-grepped every claim the spec cites; findings vs. the spec:

- `api/src/api/demo/` — present (`fixtures.ts`, `index.ts`). Removed.
- `endpoints.ts` `isDemo() ? demo.… :` branches + `VITE_DEMO` — present on every
  endpoint. Stripped.
- `features/sparks/index.tsx` `agentAttributed={s.rule === 'simultaneous-heat-cool'}`
  — present. The wire `Spark` (types.ts) has no agent-attribution field, so the
  chip was faked. Dropped per spec ("Do not fake it"). No TODO raised: product
  did not ask for it back; if it returns it is a backend field.
- `profile-dropdown.tsx` "satnaing" / `satnaingdev@gmail.com` — present. Replaced
  with a neutral operator identity ("Operator" / "Signed in locally", fallback
  `OP`) until auth lands (UI-07). Removed the shadcn `AvatarImage` placeholder.
- `components/layout/data/sidebar-data.ts` — already neutral ("Operator", empty
  email/avatar); no satnaing remnant. Spec note about a user block there had
  already been resolved by an earlier row; left as-is.
- `sample-board` — still referenced by `features/flows/index.tsx`. Owned by UI-04
  per the spec exception; excluded from the grep gate and noted here.

## Task breakdown / commits

1. `endpoints.ts`: rewrote each endpoint as a plain `request()` call; deleted the
   `demo`/`isDemo` import and the demo-mode docstring. `git rm` of
   `api/demo/fixtures.ts` + `api/demo/index.ts`. Cleaned the now-dead
   `VITE_DEMO=0` stub from `wire-contract.unit.test.ts` (the test no longer needs
   to force live mode since live is the only mode).
2. Sparks: removed the fake `agentAttributed` prop from `sparks/index.tsx` and
   from both `spark-list-item.tsx` and `spark-row.tsx` (prop + chip + now-unused
   `Sparkles`/`Badge` imports), since no real wire field feeds it.
3. `profile-dropdown.tsx`: neutral operator identity, dropped placeholder avatar.
4. `scripts/screenshot.mjs`: updated stale `VITE_DEMO=0` dev-loop instructions
   (real mode is now the only mode).

Task 4 of the spec (empty/error-state polish) was already satisfied: lists/charts
keep skeletons (loading states unchanged), and the global `QueryCache.onError` in
`main.tsx` already routes server-down errors to sonner — no white-screen. No new
code needed there; verified, not stubbed.

## Gates

- `pnpm -C rubix/ui build` — green (tsc -b + vite build).
- `pnpm -C rubix/ui test:unit` — green, 18/18.
- Grep gate (live this row):
  `grep -rn "api/demo\|VITE_DEMO\|sample-board" rubix/ui/src --include='*.ts*'`
  → only hit is `features/flows/index.tsx` `sample-board` import (UI-04-owned,
  excluded per spec). Zero `api/demo` / `VITE_DEMO` hits anywhere.
- Look-freeze — VERIFIED. Built and ran `rubix --seed-dev` (:8088, real seeded
  store: sites/equips/points/sparks present), `pnpm dev` (:5181 here, 5180 busy
  with another session's server), captured all four surfaces with
  `scripts/screenshot.mjs` (playwright) and eyeballed against
  `docs/reference/`. Dashboard and sparks (the surfaces this row touched) are
  pixel-equivalent: every widget, chart series, and the master-list density are
  retained. The only deltas are live relative timestamps (25s→44-55s ago), the
  intended satnaing→Operator/SN→OP identity change, and the removal of the faked
  `agent` chip on the `simultaneous-heat-cool` spark — all expected. points/flows
  unchanged. The committed UI-02 reference PNGs were preserved (re-shoots compared
  out-of-tree; baseline restored, not overwritten).

## Assumptions / follow-ups

- Kept the optional empty-state copy and global error toast as-is — they were
  already production-grade.
- No TODOs.md entries raised.
